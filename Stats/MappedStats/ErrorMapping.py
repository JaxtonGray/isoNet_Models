# Mapping the error for a specific model, given within the comand console when the program is run

# Importing the necessary libraries
import sys
import numpy
import pandas as pd
import geopandas as gpd
import matplotlib.pyplot as plt

# Create a function to read in a modelNum and load in the data and 